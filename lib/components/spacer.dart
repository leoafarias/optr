import 'package:flutter/material.dart';

class OptrSpacer extends StatelessWidget {
  const OptrSpacer();

  @override
  Widget build(BuildContext context) {
    const size = 20.0;
    return const SizedBox(height: size, width: size);
  }
}
